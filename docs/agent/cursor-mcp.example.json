{
  "_comment": "Copy this file as `.cursor/mcp.json` (Cursor) or merge into your client's MCP config. The package is on PyPI (https://pypi.org/project/murindi-mcp/); `uvx` will download and cache it on first run. Replace the agent token. See README and docs/agent/AGENT_QUICKSTART.md for how to issue the token.",
  "mcpServers": {
    "murindi": {
      "command": "uvx",
      "args": ["murindi-mcp"],
      "env": {
        "MURINDI_BASE_URL": "http://localhost:8888",
        "MURINDI_AGENT_TOKEN": "qd_agent_REPLACE_ME"
      }
    }
  }
}
